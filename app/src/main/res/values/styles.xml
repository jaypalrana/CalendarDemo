<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BaseStructure" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BaseStructure" parent="Base.Theme.BaseStructure" />


    <style name="card_view_style"  parent="Base.CardView">
        <item name="android:elevation">@dimen/_8sdp</item>
        <item name="android:clipToPadding">true</item>
        <item name="android:divider">@android:color/transparent</item>
        <item name="android:layout_marginStart">@dimen/_6sdp</item>
        <item name="android:layout_marginEnd">@dimen/_6sdp</item>
        <item name="android:layout_marginTop">@dimen/_6sdp</item>
        <item name="cardCornerRadius">@dimen/_5sdp</item>
        <item name="android:dividerHeight">0.0px</item>
        <item name="android:clipChildren">false</item>
    </style>
</resources>